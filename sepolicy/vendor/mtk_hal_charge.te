type mtk_hal_charge, domain;
type mtk_hal_charge_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(mtk_hal_charge)
add_hwservice(mtk_hal_charge, mtk_hal_charge_hwservice)

hwbinder_use(mtk_hal_charge)

get_prop(mtk_hal_charge, hwservicemanager_prop)

